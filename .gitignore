__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/

# NOTE: data/resources.db IS committed on purpose — it carries found_at history
# (first-seen dates power "new this week") so the daily CI refresh and local
# discovery share one source of truth across machines.

# Runtime artifacts from the scheduled jobs.
data/daily.log
data/discover.log
data/digest-latest.txt

# macOS
.DS_Store
