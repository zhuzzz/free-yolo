__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/

# Local catalog DB (regenerate with `freeyolo collect`).
# Commit it instead if you want found_at history to persist across machines.
data/resources.db

# Runtime artifacts from the scheduled jobs.
data/daily.log
data/discover.log
data/digest-latest.txt

# macOS
.DS_Store
